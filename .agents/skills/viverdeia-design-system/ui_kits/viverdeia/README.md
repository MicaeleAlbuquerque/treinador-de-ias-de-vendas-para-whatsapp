# Viver de IA — Marketing UI Kit

Recreation of viverdeia.ai-style marketing surfaces. Static — cosmetic only.

**What's here**
- `index.html` — single scrollable landing page: nav → hero → social proof → feature grid → testimonial band → pricing → CTA → footer.
- `Nav.jsx`, `Hero.jsx`, `Features.jsx`, `Proof.jsx`, `Testimonials.jsx`, `Pricing.jsx`, `CTA.jsx`, `Footer.jsx`.

**Notes**
- O lockup vem de `assets/viverdeia-lockup-black.svg` (nav e footer sobre fundo claro). Pra uso sobre fundo escuro, trocar por `assets/viverdeia-lockup-white.svg`. Versões só-ícone em `assets/viverdeia-icon-black.svg` / `-white.svg`.type.css` carry the visual weight.
